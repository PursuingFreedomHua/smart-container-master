server:
  port: 9966
  servlet:
    context-path: /kefu

spring:
  application:
    name: container-kefu
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.16.1:8848
      config:
        server-addr: 192.168.16.1:8848
        file-extension: yaml
        group: DEFAULT_GROUP

logging:
  level:
    root: info
